import {
  select,
  axisBottom,
  axisLeft,
  bisectLeft,
  extent,
  line,
  max,
  mouse,
  scaleLinear,
  scaleUtc,
  tsv,
  utcParse
} from 'd3'

import {least} from 'd3-array'

export default {
  select,
  axisBottom,
  axisLeft,
  bisectLeft,
  extent,
  line,
  max,
  mouse,
  scaleLinear,
  scaleUtc,
  tsv,
  utcParse,
  least
};
